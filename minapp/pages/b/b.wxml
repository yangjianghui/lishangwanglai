<!--pages/b/b.wxml-->
<view class="box" wx:for="{{array.list}}">
  <view class="left">{{item.index}}/10</view>
  <view class="zhu">{{item.title}}</view>
  <view wx:for="{{item.a}}" wx:key="*this">
    <view class="la">{{item}}</view>
  </view>
</view>
